# Logs - January 14, 2026

> [!NOTE]
> This is AI-generated but are based on git commits + manual edits

---

## Summary

---

### Fix Environment Variables Mismatch

Problem:
Newer environment variables were not backed up, therefore manual sourcing was done. Google OAuth requires GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET, as well as the new environment variables required by the new V2 of LSCS Links API.

MONGO_URI was used instead of DB_URL.

Solution:
Add the new environment variables (see .env.example)

```env
DB_NAME=
DB_URL=
JWT_SECRET=
PORT=3000
GOOGLE_CLIENT_ID=<MUST_BE_THE_SAME_AS_ONE_IN_LSCS_LINKS_WEB>
GOOGLE_CLIENT_SECRET=<MUST_BE_THE_SAME_AS_ONE_IN_LSCS_LINKS_WEB>
LSCS_API_KEY=<GENERATE_VIA_LSCS_CORE>
```

Map the correct environment variables properly.

> [!IMPORTANT]
> Make sure to match this GOOGLE_CLIENT_ID with the one used in LSCS Links API (Google `aud` field)

---

### Fix Undefined Behavior and Type Errors

**Timestamp:** 22:57:44
**Problem:**

- TypeScript errors regarding `req.member` not being recognized.
- Potential undefined behavior when accessing `req.params.shortlink`.
  **Solution:**
- Created `src/types/express.d.ts` to extend the Express `Request` interface globally.
- Cleaned up inline type declarations in `auth.middleware.ts`.
- Added safety checks and type casting for `shortlink` in `link.controller.ts`.
  **Affected Files:**
- `src/app.ts`
- `src/controllers/link.controller.ts`
- `src/middlewares/auth.middleware.ts`
- `src/types/express.d.ts`

---

### Add pnpm Support

**Timestamp:** 22:57:48
**Problem:** Missing lockfile for pnpm, preventing deterministic installs with pnpm.
**Solution:** Generated and committed `pnpm-lock.yaml`.
**Affected Files:**

- `pnpm-lock.yaml`

---

### Fix ts-node Incompatibility

**Timestamp:** 23:02:19
**Problem:**

- `ts-node` was causing execution issues in the dev environment.
- Type augmentation for Express `Request` was not picking up correctly with `declare global`.
  **Solution:**
- Switched dev script to compile with `tsc` and run with `node` (`tsc && node dist/app.js`) instead of using `ts-node`.
- Removed `ts-node` dependency.
- Updated `src/types/express.d.ts` to use `declare module 'express'` for correct module augmentation.
  **Affected Files:**
- `package.json`
- `package-lock.json`
- `pnpm-lock.yaml`
- `src/types/express.d.ts`

---

### Add Logging and Adjust Admin Routes

**Timestamp:** 23:18:34
**Problem:**

- Lack of visibility into request processing (need for debugging).
- Issues with admin route resolution.
  **Solution:**
- Added logging statements to multiple routes and middlewares.
- Attempted to fix admin routing by explicitly prefixing paths with `/admin` (later found to be incorrect).
  **Affected Files:**
- `src/config/db.ts`
- `src/middlewares/auth.middleware.ts`
- `src/middlewares/rbac.middleware.ts`
- `src/routes/admin.routes.ts`
- `src/routes/analytics.routes.ts`
- `src/routes/links.routes.ts`
- `src/routes/user.routes.ts`

---

### Fix Admin Routes 404

**Timestamp:** 23:41:35
**Problem:** Admin routes were returning 404 errors. The previous change to add `/admin` prefix inside the router caused double nesting (e.g., `/admin/admin/create`) if the router was already mounted at `/admin`.
**Solution:** Removed the `/admin` prefix from routes within `admin.routes.ts`.
**Affected Files:**

- `src/routes/admin.routes.ts`
